/**
 * Created by woojin on 2016. 3. 13.
 */

import {bootstrap} from 'angular2/platform/browser'
import {RCPAppCmp} from './app.cmp'

// webapp은 angular2/platform/browser 모듈의 bootstrap 함수를 사용해서 시작 할 수 있다.
bootstrap(RCPAppCmp);